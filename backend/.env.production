# Production Environment Variables for Railway

# Solana RPC (usa Helius o QuickNode en producción)
RPC_ENDPOINT=https://api.mainnet-beta.solana.com
# Para mejor performance, usa un RPC privado:
# RPC_ENDPOINT=https://rpc.helius.xyz/?api-key=TU_API_KEY

# Network
NODE_ENV=production

# Database (Railway proveerá esto automáticamente)
# DATABASE_URL se configura automáticamente en Railway

# Server
PORT=3001

# CORS (tu dominio de frontend)
ALLOWED_ORIGINS=https://degenagent.fun,https://www.degenagent.fun

# Comisiones
COMMISSION_RATE=0.5
REFERRAL_COMMISSION_RATE=10
MIN_TRADE_FOR_COMMISSION=10

# Admin wallet (tu wallet para recibir comisiones)
ADMIN_WALLET_ADDRESS=TU_WALLET_PUBLICA_AQUI

# Encryption key para guardar private keys (genera uno seguro)
ENCRYPTION_KEY=GENERA_UNA_KEY_SEGURA_DE_32_CARACTERES

# Jupiter API (opcional si necesitas rate limits más altos)
# JUPITER_API_KEY=tu_api_key_aqui

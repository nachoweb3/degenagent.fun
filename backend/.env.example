PORT=3001
RPC_ENDPOINT=https://api.devnet.solana.com
FACTORY_PROGRAM_ID=Factory11111111111111111111111111111111111
MANAGER_PROGRAM_ID=Manager11111111111111111111111111111111111
TREASURY_WALLET=YOUR_TREASURY_WALLET_HERE

# Frontend URL for referral links (defaults to https://degenagent.fun in production)
FRONTEND_URL=https://degenagent.fun

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=https://degenagent.fun,http://localhost:3000

# Security: Encryption Master Key (REQUIRED for secure key storage)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: Keep this secret! Store in AWS Secrets Manager in production
ENCRYPTION_MASTER_KEY=GENERATE_YOUR_OWN_32_BYTE_HEX_KEY_HERE

# AWS KMS (alternative to local encryption for production)
AWS_REGION=us-east-1
AWS_KMS_KEY_ID=your-kms-key-id

# Database (optional for MVP)
DATABASE_URL=postgresql://user:password@localhost:5432/agentfun
